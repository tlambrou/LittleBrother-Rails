<h2><%= Rubric.find(@criterium.rubric_id).topic %></h2>
<div class="row">
  <div class="col-lg-6">
      <div class="panel panel-default">
          <div class="panel-heading">
            <%= @criterium.name %>
          </div>
          <!-- /.panel-heading -->
          <div class="panel-body">
              <%= @criterium.description %>

          </div>
          <!-- /.panel-body -->
          <div class="panel-body">
              <% @criterium.children.each do |r| %>
              <%= link_to r.name, r %>
              <% end %>

          </div>
      </div>
      <!-- /.panel -->
  </div>
